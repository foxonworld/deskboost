# Testing and Memory Rules

- Prefer lint, typecheck, targeted tests, and smoke checks.
- Use Playwright/Vitest/Jest only when available in the repo.
- Do not invent tools or scripts.
- Avoid massive test generation by default.
- Agentmemory should store durable architecture decisions, conventions, recurring patterns, project summaries, and debugging learnings only.
